services:

  backend:
    build:
      context: ./backend
      dockerfile: ../Docker/Dockerfile.backend
    env_file:
      - ./backend/.env
    
  frontend:
    build:
      context: ./frontend
      dockerfile: ../Docker/Dockerfile.frontend
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
